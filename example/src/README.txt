These examples show how to construct adaptors and use openadaptor
components in java code. Typically they mirror the spring examples.

Rather than duplicate all of the examples as both code and spring config
files. We choose to show the majority as spring examples. That way
people don't have to compile code to try things out.
